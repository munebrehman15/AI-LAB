#a Python program to find if a given string starts with a given character using Lambda.
begins = lambda x: \
    True \
        if x.startswith('M') \
        else False
print(begins('Muneeb'))
begins = lambda x: \
    True \
        if x.startswith('M') \
        else False
print(begins('Rehman'))